package ch02.코틀린기초

/**
 * 코틀린에서는 타입 지정을 생략하는 경우가 흔하다.
 * 타입을 명시할 때는 변수 이름 뒤에 한다.
 */
val question = "삶, 우주, 그리고 모든 것에 대한 궁긍적인 질문"

val answer = 42

val answer2: Int = 42

/**
 * 초기화 식을 사용하지 않고 변수를 선언ㄴ하려면 변수 타입을 반드시 명시해야 한다.
 */

// val answer3 // error
